# count キー追加 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `validate()` が生成するメタデータに `count`（table の rows 件数）を追加し、`group()` と `expand()` も rows 件数が変わるたびに `count` を再計算する。

**Architecture:** TDD で関数単位に実装する。`validate()` → `group()` → `expand()` の順に、失敗テスト追加 → 実装 → 通過確認 → コミット を繰り返す。`orderby()` は rows 件数を変えないため `count` 再計算不要。`detach()` と `attach()` は変更なし。

**Tech Stack:** Perl 5, Test::More

---

## ファイル構成

| ファイル | 変更内容 |
|---|---|
| `test/tabletools.t` | `validate()` / `group()` / `expand()` / `orderby()` に `count` アサーションを追加 |
| `src/TableTools.pm` | `validate()` の meta 生成・`group()` の meta 生成・`expand()` の meta 生成・冒頭 Rules コメントを更新 |
| `docs/design/design-concept.md` | `count` を Terms・例・方針・Rules に追記し、To Do セクションを削除 |
| `docs/spec.md` | メタデータの説明と各 API の説明に `count` を追加 |
| `docs/test-spec.md` | テスト仕様の期待結果に `count` を追記 |

---

## Task 1: `validate()` と `orderby()` の失敗テストを追加する

**Files:**
- Modify: `test/tabletools.t`

- [ ] **Step 1: `validate: cols なし・正常` に count アサーションを追加する**

`test/tabletools.t` の `subtest 'validate: cols なし・正常'` 内（`'B は str'` の行の直後）に以下を挿入する：

```perl
    is($table->[0]{'#'}{count}, 2,        'count が 2');
```

変更後のサブテスト全体：
```perl
subtest 'validate: cols なし・正常' => sub {
    my $rows  = [{A => 1, B => 'x'}, {A => 2, B => 'y'}];
    my $table = validate($rows);

    is(scalar @$table, 3, 'meta + データ2行 = 3要素');
    ok(exists $table->[0]{'#'},           '先頭はメタデータ行');
    ok(exists $table->[0]{'#'}{attrs},    'attrs が存在する');
    ok(!exists $table->[0]{'#'}{order},   'order は存在しない');
    is($table->[0]{'#'}{attrs}{A}, 'num', 'A は num');
    is($table->[0]{'#'}{attrs}{B}, 'str', 'B は str');
    is($table->[0]{'#'}{count}, 2,        'count が 2');
    is($table->[1]{A}, 1,                 'データ1行目が正しい');
};
```

- [ ] **Step 2: `validate: cols あり・メタデータ付きで返す` に count アサーションを追加する**

`subtest 'validate: cols あり・メタデータ付きで返す'` 内（`'C は num'` の行の直後）に以下を挿入する：

```perl
    is($meta->{count}, 2, 'count が 2');
```

変更後のサブテスト全体：
```perl
subtest 'validate: cols あり・メタデータ付きで返す' => sub {
    my $rows  = [{A => 1, B => 'foo', C => 3}, {A => 2, B => 'bar', C => 4}];
    my $table = validate($rows, ['A', 'B', 'C']);

    is(scalar @$table, 3, 'meta + データ2行 = 3要素');
    ok(exists $table->[0]{'#'}, '先頭はメタデータ行');

    my $meta = $table->[0]{'#'};
    is_deeply($meta->{order}, ['A', 'B', 'C'], 'order が正しい');
    is($meta->{attrs}{A}, 'num', 'A は num');
    is($meta->{attrs}{B}, 'str', 'B は str');
    is($meta->{attrs}{C}, 'num', 'C は num');
    is($meta->{count}, 2, 'count が 2');

    is($table->[1]{A}, 1, 'データ1行目が正しい');
    is($table->[2]{A}, 2, 'データ2行目が正しい');
};
```

- [ ] **Step 3: `orderby: メタデータを引き継ぐ` に count アサーションを追加する**

`subtest 'orderby: メタデータを引き継ぐ'` 内（`'attrs B が引き継がれている'` の行の直後）に以下を挿入する：

```perl
    is($sorted->[0]{'#'}{count}, 2,          'count が引き継がれている');
```

変更後のサブテスト全体：
```perl
subtest 'orderby: メタデータを引き継ぐ' => sub {
    my $table  = validate([{A => 2, B => 'x'}, {A => 1, B => 'y'}], ['A', 'B']);
    my $sorted = orderby($table, ['A']);
    ok(exists $sorted->[0]{'#'},                    '先頭にメタデータ行がある');
    ok(exists $sorted->[0]{'#'}{order},             'order が引き継がれている');
    is_deeply($sorted->[0]{'#'}{order}, ['A', 'B'], 'order の内容が正しい');
    is($sorted->[0]{'#'}{attrs}{A}, 'num',          'attrs A が引き継がれている');
    is($sorted->[0]{'#'}{attrs}{B}, 'str',          'attrs B が引き継がれている');
    is($sorted->[0]{'#'}{count}, 2,                 'count が引き継がれている');
};
```

- [ ] **Step 4: テストを実行して失敗を確認する**

```
perl test/tabletools.t
```

期待：`count が 2` と `count が引き継がれている` の 3 箇所が FAIL する。それ以外は PASS のまま。

---

## Task 2: `validate()` に `count` を実装してテストを通す

**Files:**
- Modify: `src/TableTools.pm`

- [ ] **Step 1: `validate()` の meta 生成に `count` を追加する**

`src/TableTools.pm` の `validate()` 内（`my $new_meta = ...` の行）を変更する：

変更前：
```perl
    my $new_meta = {'#' => {attrs => $attrs}};
    $new_meta->{'#'}{order} = $order if $order;
```

変更後：
```perl
    my $new_meta = {'#' => {attrs => $attrs, count => scalar(@$rows)}};
    $new_meta->{'#'}{order} = $order if $order;
```

- [ ] **Step 2: 冒頭 Rules コメントに `count` を追加する**

`src/TableTools.pm` の Rules コメント末尾（`# expand() は group() 済みの table を平坦化して table を返す` の行の直後）に以下を追加する：

```perl
# count は validate() が生成する meta に必ず含まれ、table の rows 件数を表す
# orderby() は行数不変なので meta をそのまま維持する
# group() と expand() は rows 件数が変わるので count を再計算した新 meta を生成する
```

- [ ] **Step 3: テストを実行して通過を確認する**

```
perl test/tabletools.t
```

期待：全テスト PASS。Task 1 で追加した 3 つの count アサーションが PASS に変わること。

- [ ] **Step 4: コミットする**

```bash
git add src/TableTools.pm test/tabletools.t
git commit -m "feat: validate() の meta 生成に count を追加し orderby テストを更新"
```

---

## Task 3: `group()` の失敗テストを追加する

**Files:**
- Modify: `test/tabletools.t`

- [ ] **Step 1: `group: 1段グループ化` に count アサーションを追加する**

`subtest 'group: 1段グループ化'` の末尾（`'A=2 の子は1件'` の行の直後、`};` の前）に以下を挿入する：

```perl
    is($grouped->[0]{'#'}{count}, 2, 'count がトップレベルグループ数（2）と一致する');
```

変更後のサブテスト全体：
```perl
subtest 'group: 1段グループ化' => sub {
    my $table = validate(
        [{A => 1, B => 'x', C => 10}, {A => 1, B => 'y', C => 20}, {A => 2, B => 'x', C => 30}],
        ['A', 'B', 'C'],
    );
    my $grouped = group($table, ['A']);

    is(scalar @$grouped, 3, 'meta + グループ2件 = 3要素');
    ok(exists $grouped->[0]{'#'}, '先頭はメタデータ行');
    is($grouped->[1]{A}, 1, '1グループ目は A=1');
    is(scalar @{$grouped->[1]{'@'}}, 2, 'A=1 の子は2件');
    is($grouped->[1]{'@'}[0]{B}, 'x', '子の1行目 B=x');
    is($grouped->[1]{'@'}[0]{C}, 10,  '子の1行目 C=10');
    ok(!exists $grouped->[1]{'@'}[0]{A}, '子行に A は含まれない');

    is($grouped->[2]{A}, 2, '2グループ目は A=2');
    is(scalar @{$grouped->[2]{'@'}}, 1, 'A=2 の子は1件');
    is($grouped->[0]{'#'}{count}, 2, 'count がトップレベルグループ数（2）と一致する');
};
```

- [ ] **Step 2: `group: 2段グループ化` に count アサーションを追加する**

`subtest 'group: 2段グループ化'` の末尾（`'孫行に B は含まれない'` の行の直後、`};` の前）に以下を挿入する：

```perl
    is($grouped->[0]{'#'}{count}, 2, 'count がトップレベルグループ数（2）と一致する');
```

変更後のサブテスト全体：
```perl
subtest 'group: 2段グループ化' => sub {
    my $table = validate(
        [{A => 1, B => 'x', C => 10}, {A => 1, B => 'x', C => 20},
         {A => 1, B => 'y', C => 30}, {A => 2, B => 'x', C => 40}],
        ['A', 'B', 'C'],
    );
    my $grouped = group($table, ['A'], ['B']);

    is(scalar @$grouped, 3, 'meta + トップレベル2グループ = 3要素');
    is($grouped->[1]{A}, 1, 'A=1 が先');

    my $a1_children = $grouped->[1]{'@'};
    is(scalar @$a1_children, 2, 'A=1 の子グループは2件');
    is($a1_children->[0]{B}, 'x', '先にB=x');
    is($a1_children->[1]{B}, 'y', '次にB=y');

    my $bx_children = $a1_children->[0]{'@'};
    is(scalar @$bx_children, 2,  'B=x の孫は2件');
    is($bx_children->[0]{C}, 10, '孫1: C=10');
    is($bx_children->[1]{C}, 20, '孫2: C=20');

    ok(!exists $bx_children->[0]{A}, '孫行に A は含まれない');
    ok(!exists $bx_children->[0]{B}, '孫行に B は含まれない');
    is($grouped->[0]{'#'}{count}, 2, 'count がトップレベルグループ数（2）と一致する');
};
```

- [ ] **Step 3: テストを実行して失敗を確認する**

```
perl test/tabletools.t
```

期待：`count がトップレベルグループ数（2）と一致する` の 2 箇所が FAIL する。
- `group: 1段グループ化` では `3` rows から `2` グループになるため、現時点の count（`validate()` 由来の `3`）と一致しない
- `group: 2段グループ化` では `4` rows から `2` グループになるため、count（`validate()` 由来の `4`）と一致しない

---

## Task 4: `group()` に `count` を実装してテストを通す

**Files:**
- Modify: `src/TableTools.pm`

- [ ] **Step 1: `group()` の末尾を `count` 込みの新 meta に変更する**

`src/TableTools.pm` の `group()` 内（末尾の `return attach($grouped, $meta);`）を変更する：

変更前：
```perl
    # attach() で meta を戻して返す
    return attach($grouped, $meta);
```

変更後：
```perl
    # count を再計算した新 meta を生成して返す
    my $new_meta = {'#' => {attrs => $attrs, count => scalar(@$grouped)}};
    $new_meta->{'#'}{order} = $meta->{'#'}{order} if $meta->{'#'}{order};
    return attach($grouped, $new_meta);
```

- [ ] **Step 2: テストを実行して通過を確認する**

```
perl test/tabletools.t
```

期待：全テスト PASS。Task 3 で追加した 2 つの count アサーションが PASS に変わること。

- [ ] **Step 3: コミットする**

```bash
git add src/TableTools.pm test/tabletools.t
git commit -m "feat: group() の meta 生成に count（トップレベルグループ数）を追加"
```

---

## Task 5: `expand()` の失敗テストを追加する

**Files:**
- Modify: `test/tabletools.t`

- [ ] **Step 1: `expand: 1段グループ化を戻す` に count アサーションを追加する**

`subtest 'expand: 1段グループ化を戻す'` の末尾（`'1行目 C=10'` の行の直後、`};` の前）に以下を挿入する：

```perl
    is($flat->[0]{'#'}{count}, 3, 'count が展開後行数（3件）に戻る');
```

変更後のサブテスト全体：
```perl
subtest 'expand: 1段グループ化を戻す' => sub {
    my $original = validate(
        [{A => 1, B => 'x', C => 10}, {A => 1, B => 'y', C => 20}, {A => 2, B => 'x', C => 30}],
        ['A', 'B', 'C'],
    );
    my $grouped  = group($original, ['A']);
    my $flat     = expand($grouped);

    is(scalar @$flat, 4, 'meta + 元の3行 = 4要素');
    ok(exists $flat->[0]{'#'}, '先頭はメタデータ行');
    is($flat->[1]{A}, 1,   '1行目 A=1');
    is($flat->[1]{B}, 'x', '1行目 B=x');
    is($flat->[1]{C}, 10,  '1行目 C=10');
    is($flat->[0]{'#'}{count}, 3, 'count が展開後行数（3件）に戻る');
};
```

- [ ] **Step 2: `expand: 2段グループ化を戻す` に count アサーションを追加する**

`subtest 'expand: 2段グループ化を戻す'` の末尾（`'4行目 A=2'` の行の直後、`};` の前）に以下を挿入する：

```perl
    is($flat->[0]{'#'}{count}, 4, 'count が展開後行数（4件）に戻る');
```

変更後のサブテスト全体：
```perl
subtest 'expand: 2段グループ化を戻す' => sub {
    my $original = validate(
        [{A => 1, B => 'x', C => 10}, {A => 1, B => 'x', C => 20},
         {A => 1, B => 'y', C => 30}, {A => 2, B => 'x', C => 40}],
        ['A', 'B', 'C'],
    );
    my $grouped = group($original, ['A'], ['B']);
    my $flat    = expand($grouped);

    is(scalar @$flat, 5, 'meta + 元の4行 = 5要素');
    ok(exists $flat->[0]{'#'}, '先頭はメタデータ行');
    is($flat->[1]{A}, 1,   '1行目 A=1');
    is($flat->[1]{B}, 'x', '1行目 B=x');
    is($flat->[1]{C}, 10,  '1行目 C=10');
    is($flat->[4]{A}, 2,   '4行目 A=2');
    is($flat->[0]{'#'}{count}, 4, 'count が展開後行数（4件）に戻る');
};
```

- [ ] **Step 3: テストを実行して失敗を確認する**

```
perl test/tabletools.t
```

期待：`count が展開後行数（3件）に戻る` と `count が展開後行数（4件）に戻る` の 2 箇所が FAIL する。
- `group()` が count=2 の meta を生成しており、`expand()` はその meta をそのまま `attach()` するため count=2 のまま

---

## Task 6: `expand()` に `count` を実装してテストを通す

**Files:**
- Modify: `src/TableTools.pm`

- [ ] **Step 1: `expand()` の末尾を `count` 込みの新 meta に変更する**

`src/TableTools.pm` の `expand()` 内（末尾の `return attach(\@flat, $meta);`）を変更する：

変更前：
```perl
    # attach() で meta を戻して返す
    return attach(\@flat, $meta);
```

変更後：
```perl
    # count を再計算した新 meta を生成して返す
    my $new_meta = {'#' => {attrs => $meta->{'#'}{attrs}, count => scalar(@flat)}};
    $new_meta->{'#'}{order} = $meta->{'#'}{order} if $meta->{'#'}{order};
    return attach(\@flat, $new_meta);
```

- [ ] **Step 2: テストを実行して通過を確認する**

```
perl test/tabletools.t
```

期待：全テスト PASS。Task 5 で追加した 2 つの count アサーションが PASS に変わること。

- [ ] **Step 3: コミットする**

```bash
git add src/TableTools.pm test/tabletools.t
git commit -m "feat: expand() の meta 生成に count（展開後行数）を追加"
```

---

## Task 7: ドキュメントを更新する

**Files:**
- Modify: `docs/design/design-concept.md`
- Modify: `docs/spec.md`
- Modify: `docs/test-spec.md`

- [ ] **Step 1: `docs/design/design-concept.md` を更新する**

以下の変更を順に行う。

**① `order` の定義の直後に `count` の定義を追加する（`- \`order\` は列順を指定した \`validate\` のときだけ持つ` の行の直後）：**

```
- `count` は validate() が生成する meta に必ず含まれるキー
- `count` は table の rows 件数（スカラー値）を表す
- `attrs` と `count` が必須、`order` が任意
```

**② メタデータ例（`{'#' => {` から始まるコードブロック）を更新する：**

変更前：
```perl
{'#' => {
    attrs => {A => 'num', B => 'str', C => 'num'},
    order => ['A', 'B', 'C'],   # 省略可
}}
```

変更後：
```perl
{'#' => {
    attrs => {A => 'num', B => 'str', C => 'num'},
    count => 2,
    order => ['A', 'B', 'C'],   # 省略可（validate($aoh, $cols) のときだけ付く）
}}
```

**③ `table` 例の meta 部分を更新する：**

変更前：
```perl
[
    {'#' => {
        attrs => {A => 'num', B => 'str', C => 'num'},
        order => ['A', 'B', 'C'],   # 省略可
    }},
    {A => 1, B => 'x', C => 10},
    {A => 2, B => 'y', C => 20},
]
```

変更後：
```perl
[
    {'#' => {
        attrs => {A => 'num', B => 'str', C => 'num'},
        count => 2,
        order => ['A', 'B', 'C'],   # 省略可（validate($aoh, $cols) のときだけ付く）
    }},
    {A => 1, B => 'x', C => 10},
    {A => 2, B => 'y', C => 20},
]
```

**④ 方針セクションに `count` の記述を追加する（`attach() は validate() を呼ばず...` の行の直後）：**

```
- `validate()` が meta を新規生成するときは `count` を必ず含める
- `count` は table の rows 件数とする
- `orderby()` は rows 件数を変えないので、入力 table の meta をそのまま維持する
- `group()` は rows 件数が変わるので、`count` を再計算した新 meta を生成する
- `expand()` は rows 件数が変わるので、`count` を再計算した新 meta を生成する
```

**⑤ Rules コメントに `count` の行を追加する（最後の `# expand() は group() 済みの table...` の行の直後）：**

```perl
# count は validate() が生成する meta に必ず含まれ、table の rows 件数を表す
# orderby() は行数不変なので meta をそのまま維持する
# group() と expand() は rows 件数が変わるので count を再計算した新 meta を生成する
```

**⑥ `## To Do` セクション（末尾の全行）を削除する。**

- [ ] **Step 2: `docs/spec.md` を更新する**

以下の変更を行う。

**① 用語と記号セクションに `count` を追加する（`- \`order\`: カラム名の並びを表す配列リファレンス` の行の直後）：**

```
- `count`: validate() が生成する meta のキー。table の rows 件数を表す
```

**② メタデータ行セクションの箇条書き（`- \`order\` は省略可能。\`attrs\` は必須` の行）を変更する：**

変更前：
```
- `order` は省略可能。`attrs` は必須
- `order` は `validate($table, $cols)` を通じた場合のみ生成される
```

変更後：
```
- `attrs` と `count` は必須。`order` は省略可能
- `order` は `validate($table, $cols)` を通じた場合のみ生成される
```

**③ メタデータ行セクションのコード例を更新する：**

変更前：
```perl
{'#' => {attrs => {A => 'num', B => 'str'}, order => ['A', 'B', 'C']}}
```

変更後：
```perl
{'#' => {attrs => {A => 'num', B => 'str'}, count => 2, order => ['A', 'B', 'C']}}
```

**④ データ構造セクションの table 例（attrs + order 付き）を更新する：**

変更前：
```perl
# table（attrs + order）
[
    {'#' => {attrs => {A => 'num', B => 'str', C => 'num'}, order => ['A', 'B', 'C']}},
    {A => 1, B => 'foo', C => 3},
    {A => 5, B => 'bar', C => 7},
]
```

変更後：
```perl
# table（attrs + count + order）
[
    {'#' => {attrs => {A => 'num', B => 'str', C => 'num'}, count => 2, order => ['A', 'B', 'C']}},
    {A => 1, B => 'foo', C => 3},
    {A => 5, B => 'bar', C => 7},
]
```

**⑤ `group()` の説明のステップ 7 を更新する：**

変更前：
```
7. 入力のメタデータをそのまま付け直した `table` を返す
```

変更後：
```
7. `count` を再計算した新 meta（`attrs` と `order` は入力から引き継ぎ）を付けた `table` を返す
```

**⑥ `expand()` の説明のステップ 3 を更新する：**

変更前：
```
3. 入力のメタデータをそのまま付け直して返す
```

変更後：
```
3. `count` を再計算した新 meta（`attrs` と `order` は入力から引き継ぎ）を付けて返す
```

- [ ] **Step 3: `docs/test-spec.md` を更新する**

以下の変更を行う。

**① `validate` A-1 の期待結果を更新する：**

変更前：
```
| A-1 | `rows` 入力・全行キー一致 | `attrs` のみのメタデータ行を先頭に付加した `table` を返す。`order` は新たに生成しない |
```

変更後：
```
| A-1 | `rows` 入力・全行キー一致 | `attrs` と `count` を含むメタデータ行を先頭に付加した `table` を返す。`count` は data rows の件数。`order` は新たに生成しない |
```

**② `validate` B-1 の期待結果を更新する：**

変更前：
```
| B-1 | `rows` 入力・正常データ | `attrs` と `order` 両方を含むメタデータ行を先頭に付加した `table` を返す。カラム順・型情報が `$cols` 指定順で正しく付与される |
```

変更後：
```
| B-1 | `rows` 入力・正常データ | `attrs`・`count`・`order` を含むメタデータ行を先頭に付加した `table` を返す。`count` は data rows の件数。カラム順・型情報が `$cols` 指定順で正しく付与される |
```

**③ `group` テスト 4 の期待結果を更新する：**

変更前：
```
| 4 | `attrs`・`order` 付き `table` のグループ化 | 入力の `attrs`・`order` がそのまま引き継がれる |
```

変更後：
```
| 4 | `attrs`・`order` 付き `table` のグループ化 | 入力の `attrs`・`order` が引き継がれる。`count` はトップレベルグループ数になる |
```

**④ `expand` テスト 3 の期待結果を更新する：**

変更前：
```
| 3 | `attrs`・`order` 付きグループ化 `table` を展開 | 入力の `attrs`・`order` がそのまま引き継がれる |
```

変更後：
```
| 3 | `attrs`・`order` 付きグループ化 `table` を展開 | 入力の `attrs`・`order` が引き継がれる。`count` は展開後の data rows 件数になる |
```

- [ ] **Step 4: 全テストを実行して通過を確認する**

```
perl test/tabletools.t
```

期待：全テスト PASS。

- [ ] **Step 5: コミットする**

```bash
git add docs/design/design-concept.md docs/spec.md docs/test-spec.md
git commit -m "docs: count キー追加に合わせて design-concept・spec・test-spec を更新"
```
